# Configuracao do Servidor
server.port=8080

# Configuracao do Mariadb
spring.datasource.url=jdbc:mariadb://localhost:3306/escola_db
spring.datasource.username=mariadb
spring.datasource.password=M@R$&Luna15
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# Configuracao do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Configuracao do Swagger/OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# Configuracao de encoding
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# Configuracao de log
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO
